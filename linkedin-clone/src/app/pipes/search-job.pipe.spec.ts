import { SearchJobPipe } from './search-job.pipe';

describe('SearchJobPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchJobPipe();
    expect(pipe).toBeTruthy();
  });
});
